import { Typography, Box } from "@mui/material";

export const CreatePollPage = () => {
  return (
    <Box>
      <Typography variant="h4" component="h1" gutterBottom>
        Create Poll
      </Typography>
      <Typography color="text.secondary">
        Create poll form will be implemented in the next step
      </Typography>
    </Box>
  );
};
