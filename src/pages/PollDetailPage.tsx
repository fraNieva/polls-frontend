import { Typography, Box } from "@mui/material";

export const PollDetailPage = () => {
  return (
    <Box>
      <Typography variant="h4" component="h1" gutterBottom>
        Poll Detail
      </Typography>
      <Typography color="text.secondary">
        Poll detail page will be implemented in the next step
      </Typography>
    </Box>
  );
};
